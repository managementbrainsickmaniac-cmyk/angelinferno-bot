services:
  - type: web_service
    name: angelferno-bot
    env: python
    branch: main
    plan: starter
    buildCommand: python -m venv .venv && .venv/bin/python -m pip install --upgrade pip && .venv/bin/python -m pip install --no-cache-dir -r requirements.txt && .venv/bin/python -c "import requests; print('requests version', requests.__version__)"
    startCommand: ./.venv/bin/python angelinferno-bot.py
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: TELEGRAM_MODE
        value: webhook
      - key: PORT
        value: 8080
